[project]
name = "ps-octa-oce-robot"
version = "0.1.0"
description = "Only for visualization not used in main project"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "open3d>=0.19.0",
    "opencv-python>=4.13.0.92",
]
